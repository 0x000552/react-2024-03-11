//import React from 'react'
import ReactDOM from 'react-dom/client'

const rootElement = document.getElementById('root');
const rootReact = ReactDOM.createRoot(rootElement!);

rootReact.render(<p>test</p>);
